function retval = linear_kernel (x, y, other)
  # TO DO: implement linear kernel function
  # Ignorati parametrul other pentru aceasta functie
  %folosesc funcita dot pentru a
  %calcula produsul scalar
  retval = dot(x, y);
endfunction
